class messageLocator {

    // Sending message screen
    contactIcon = '.contactIcon'
    frList = '.friendListContainer'
    messageInput = '.messageInput'
    message = '.message'

}

const messagePO = new messageLocator();
export default messagePO;

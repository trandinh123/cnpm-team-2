class userInfoLocator {
    addFrBtn = '.addFrBtn'

    // Add friends screen
    userNameInput = '.username__input'
    primaryBtn = '.primaryButton'
    cancelBtn = '.cancelbtn'

    // Setting User Info screen
    settingIcon = '.settingIcon' 
    userInfoBtn = '.userInfoBtn'
    userName = '.nameAcc'
    userGender = 'ul li:nth-child(3) p.cntInf'
    nameInput = '#nameInput'
    genderOption = 'input[type="radio"]'
    updateBtn = '.update-btn'
    submitBtn = 'button[type="submit"]'
    cancelBtn = '.cancelbtn'
    


    
}

const userInfoPO = new userInfoLocator()
export default userInfoPO